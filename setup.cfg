[flake8]
max-line-length = 100
ignore = E203,W503
per-file-ignores =
    scripts/deploy_mainnet_weth.py:E402

[tool:isort]
profile = black
line_length = 100

[tool:pytest]
filterwarnings =
    ignore::DeprecationWarning:eth_abi.*:
